# Brute-force Simulator (lab-safe)

## Safety (READ THIS FIRST)
* This code is STRICTLY for lab / educational use against **localhost** or explicitly whitelisted lab VMs.
* Default target: http://127.0.0.1:5001/login (the included mock server).
* To allow external targets you must set `CAST_BF_ALLOW_EXTERNAL=true` in the environment 
## Quick start
1. Create venv and install requests, pytest:
